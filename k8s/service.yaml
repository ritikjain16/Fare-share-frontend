kind: Service
apiVersion: v1
metadata:
  name: fareshare-frontend-service
  namespace: fareshare
spec:
  selector:
    app: fareshare-frontend
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
